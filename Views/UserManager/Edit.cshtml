@model CameraView.Models.User
@{
    ViewData["Title"] = "Chỉnh sửa người dùng";
}

<h2 class="mb-4 text-primary">✏️ Chỉnh sửa người dùng</h2>

<form asp-action="Edit" method="post" class="border rounded p-4 bg-light">
    <input type="hidden" asp-for="Id" />
    <div class="mb-3">
        <label asp-for="Username" class="form-label"></label>
        <input asp-for="Username" class="form-control" />
        <span asp-validation-for="Username" class="text-danger small"></span>
    </div>
    <div class="mb-3">
        <label asp-for="Password" class="form-label"></label>
        <input asp-for="Password" class="form-control" />
        <span asp-validation-for="Password" class="text-danger small"></span>
    </div>
    <div class="mb-3">
        <label asp-for="Role" class="form-label">Quyền</label>
        <select asp-for="Role" class="form-select">
            <option value="User">User</option>
            <option value="Admin">Admin</option>
        </select>
    </div>
    <div class="text-end">
        <a asp-action="Index" class="btn btn-secondary btn-sm">⬅ Quay lại</a>
        <button type="submit" class="btn btn-warning btn-sm">💾 Cập nhật</button>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
